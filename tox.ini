# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py{36,37,38,39,310}

[testenv]
# .vim, .py, and .vader tests were created for this project.
#commands = python -m unittest discover --start-directory tests --pattern *_test.py
commands = python -c "raise SystemExit(0)"
